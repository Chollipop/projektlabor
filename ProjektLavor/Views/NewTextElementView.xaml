<UserControl x:Class="ProjektLavor.Views.NewTextElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjektLavor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox Grid.Row="1" FontSize="20" VerticalContentAlignment="Center" MinHeight="50" MinWidth="200" AcceptsReturn="True" AcceptsTab="True" Text="{Binding TextInput, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Padding="0 10" Command="{Binding NewTextElementCommand}">
                <!--<Image />-->
                ✅
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
